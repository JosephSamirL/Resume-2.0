query allProfiles {
  allProfile {
    name
    email
    summary
    title
    number
    profilePicture {
      asset {
        url
      }
    }
    link {
      name
      url
    }
  }
}
query allExperiences($limit: Int) {
  allExperience(limit: $limit) {
    nameOfCompany
    startDate
    endDate
    role
    responsibilities
  }
}
