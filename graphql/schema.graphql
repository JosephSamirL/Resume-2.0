query allProfiles {
  allProfile {
    name
    email
    summary
    link {
      name
      url
    }
  }
}
query allExperiences($limit: Int) {
  allExperience(limit: $limit) {
    nameOfCompany
    startDate
    endDate
    role
    responsibilities
  }
}
